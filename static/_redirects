# Cloudflare Pages redirects. TIN-2536: /access and /find-the-bus were folded
# into the single /contact route (reach out -> get access -> find the bus).
# Single-product history (L72 Q3-A, 2026-08-21): /contact is deleted along with
# the rest of the legacy marketing tree, so both legacy paths now fall back to
# `/`, the member-tree entry page (src/hooks.server.ts carries the equivalent
# rule for the adapter-node production origin, which is the one this repo
# actually serves from; this file is vestigial Cloudflare Pages convention —
# see the note in hooks.server.ts).
/access            /         301
/access/*          /         301
/find-the-bus      /         301
/find-the-bus/*    /         301
