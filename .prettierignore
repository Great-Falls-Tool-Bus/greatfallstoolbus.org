build
.svelte-kit
node_modules
static
*.md
pnpm-lock.yaml
flake.lock
.gstack

# Generated + hand-written SQL migrations (TIN-3817 S1). Prettier must never
# reformat these: a migration's bytes ARE its ledger hash, so a whitespace
# change to a committed file is indistinguishable from rewriting history.
drizzle
